#include <stdio.h>
/**
 * main - Print alphabets in lowercase
 * Return: o (Success)
 */

int main(void)
{
	char c;

	c = 'a';
	while
		(c <== 'z') {
			if ((c != 'q' && c != 'e') && c <='z')
				putchar(c);
		}
	putchar('\n');
	return(0)

