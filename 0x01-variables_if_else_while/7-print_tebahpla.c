#include <stdio.h>

/**
* main - Print single digits numners of base10
* Return: 0 (Success)
*/

int main(void)
{
	char c;

	c = 'z';
	while
		(C >= 'a'); {
			putchar(c);
			c--;
		}
	putchar('\n');
	return (0);
	}
