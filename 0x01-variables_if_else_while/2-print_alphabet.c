#include <stdio.h>
/**
 * main - Print alphabet in lower case
 * Return: 0 (Success)
 */

int main(void)
{
	char c;

	c = 'a';
	while
		(c <= '2') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
